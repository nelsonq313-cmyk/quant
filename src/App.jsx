import React, { useEffect, useMemo, useState } from 'react';
import {
  Activity, BarChart3, Braces, Database, FileCode2, Folder, MessageSquareText,
  Play, Plus, Search, Settings2, Sigma, Waves,
} from 'lucide-react';
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import VolatilityLab from './VolatilityLab';
import DataTerminal from './DataTerminal.jsx';
import { personalModel } from './personalDataset';

const mean=a=>a.reduce((s,v)=>s+v,0)/(a.length||1);
const sd=a=>{const m=mean(a);return Math.sqrt(mean(a.map(v=>(v-m)**2)))};
const fmtPct=n=>`${Number(n).toFixed(1)}%`;

const initialFiles={
  'nq_edge_study.py':`# QNT personal edge research\n# Draft research code only: no Python execution runtime is attached here.\n\nfrom qnt import trades, research\n\ndata = trades.load("personal_model")\nreport = research.validate(\n    data,\n    metrics=["expectancy", "drawdown", "payoff"],\n    split_by=["session", "trend", "volatility"],\n)\n\nreport.render()`,
  'monte_carlo.py':`from qnt import montecarlo\n\nmodel = montecarlo.personal(\n    win_probability=${personalModel.winProbability.toFixed(6)},\n    loss_probability=${personalModel.lossProbability.toFixed(6)},\n    breakeven_probability=${personalModel.breakevenProbability.toFixed(6)},\n)\n\n# Live Monte Carlo uses stopped account paths, posterior uncertainty\n# and conservative payoff calibration by default.\nmodel.run(paths=4000, horizon=109)`,
  'regime_map.ipynb':`# Regime research\n# No measured regime statistics are available yet.\n# First join each trade timestamp to real NQ/MNQ market context, then compare:\n# session / time of day / direction\n# trend / consolidation\n# realized volatility / volume\n# day of week / event context when verified`,
  'trade_log.csv':`source,status,count\neval,curated position ideas,${personalModel.evalPositionIdeas}\neval,winning ideas,${personalModel.evalWins}\neval,losing ideas,${personalModel.evalLosses}\neval,breakeven ideas,${personalModel.evalBreakevens}\npractice,vetted winner payoff library,${personalModel.practiceWinners}`,
  'notes.md':`# Research notes\n\n- July 22 winners that matched the normal setup sample are included.\n- Selected tilt/mechanical/very-short losses are excluded from the curated baseline.\n- Practice winners affect payoff shape, not the eval probability counts.\n- Current normalized R is not stop-defined true R.\n- Regime statistics remain unavailable until real futures context is joined.`,
};

function Stat({label,value,sub,tone=''}){return <div className="qpStat"><span>{label}</span><b className={tone}>{value}</b>{sub&&<small>{sub}</small>}</div>}
function researchSnapshot(){const r=personalModel.evalR,wins=r.filter(x=>x>0),losses=r.filter(x=>x<0);const pf=Math.abs(wins.reduce((s,v)=>s+v,0)/(losses.reduce((s,v)=>s+v,0)||-1)),ev=mean(r),tradeSharpe=sd(r)?mean(r)/sd(r):0;return {pf,ev,tradeSharpe}}
function extractCode(text){const match=String(text||'').match(/<qnt_code>([\s\S]*?)<\/qnt_code>/i);if(!match)return {display:text,code:null};return {display:String(text).replace(match[0],'').trim(),code:match[1].replace(/^\n|\n$/g,'')}}

function Workspace(){
  const [activeFile,setActiveFile]=useState('nq_edge_study.py'),[files,setFiles]=useState(initialFiles),[prompt,setPrompt]=useState('');
  const [messages,setMessages]=useState([{role:'assistant',text:'Personal model loaded. Ask me to analyze the dataset, explain a metric, design a study, compare assumptions, or propose an editor change.'}]);
  const [loading,setLoading]=useState(false),[proposal,setProposal]=useState(null),[runState,setRunState]=useState({status:'ready',message:'Draft workspace · no Python runtime attached'});
  const stats=useMemo(researchSnapshot,[]),equity=useMemo(()=>{let x=100000;return personalModel.evalR.map((r,i)=>{x+=r*500;return {i:i+1,equity:x}})},[]);
  const suggestions=['Explain my modeled expectancy','Which assumptions affect P(ruin) most?','Compare raw vs conservative payoff calibration','What data is missing for regime analysis?'];
  const setCode=value=>setFiles(prev=>({...prev,[activeFile]:value}));
  const addFile=()=>{let i=1,name=`scratch_${i}.py`;while(files[name]){i+=1;name=`scratch_${i}.py`}setFiles(prev=>({...prev,[name]:'# New research draft\n'}));setActiveFile(name)};
  const stageActive=()=>{setRunState({status:'running',message:'Validating research draft…'});window.setTimeout(()=>setRunState({status:'done',message:'Draft staged · execution requires a connected runtime'}),250)};
  const send=async(override=null)=>{
    const text=String(override??prompt).trim();if(!text||loading)return;const prior=messages;setMessages([...prior,{role:'user',text}]);setPrompt('');setProposal(null);setLoading(true);
    try{
      const researchContext=[`Curated eval ideas: ${personalModel.evalPositionIdeas}.`,`Observed eval probabilities: win ${(personalModel.winProbability*100).toFixed(1)}%, loss ${(personalModel.lossProbability*100).toFixed(1)}%, breakeven ${(personalModel.breakevenProbability*100).toFixed(1)}%.`,`Payoff library: ${personalModel.evalWins} eval winners + ${personalModel.practiceWinners} vetted practice winners; loss library: ${personalModel.evalLosses} remaining eval losses.`,personalModel.curationNote,'Current normalized payoff units are not true stop-defined R. Conservative calibration is preferred for long simulations.','Regime results are unavailable until real futures context is joined to trade timestamps.'].join('\n');
      const screenContext={module:'Workspace',activeFile,editorType:activeFile.split('.').pop(),workspace:'NQ edge research',runtime:'draft-only',personalModel:{evalIdeas:personalModel.evalPositionIdeas,winProbability:personalModel.winProbability,lossProbability:personalModel.lossProbability,breakevenProbability:personalModel.breakevenProbability}};
      const res=await fetch('/api/copilot',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,history:prior,activeFile,code:files[activeFile],researchContext,screenContext})});const data=await res.json();if(!res.ok)throw new Error(data.error||'Copilot request failed');const parsed=extractCode(data.text);setMessages(prev=>[...prev,{role:'assistant',text:parsed.display||'I prepared an editor update.'}]);if(parsed.code)setProposal({file:activeFile,code:parsed.code});
    }catch(error){setMessages(prev=>[...prev,{role:'assistant',text:`Copilot error: ${error.message}`}])}finally{setLoading(false)}
  };
  const applyProposal=()=>{if(!proposal)return;setFiles(prev=>({...prev,[proposal.file]:proposal.code}));setActiveFile(proposal.file);setMessages(prev=>[...prev,{role:'assistant',text:`Applied the proposed replacement to ${proposal.file}.`}]);setProposal(null)};
  useEffect(()=>{const onTerminalPrompt=e=>{const text=e.detail?.prompt;if(text)send(text)};window.addEventListener('qnt:copilot-prompt',onTerminalPrompt);return()=>window.removeEventListener('qnt:copilot-prompt',onTerminalPrompt)},[messages,loading,activeFile,files]);
  return <div className="qpWorkspace"><aside className="qpFiles"><div className="qpPaneHead"><span>EXPLORER</span><button onClick={addFile} title="New file"><Plus size={13}/></button></div><div className="qpProjectName"><Folder size={13}/> NQ edge research</div><div className="qpFileList">{Object.keys(files).map(name=><button key={name} className={activeFile===name?'active':''} onClick={()=>setActiveFile(name)}><FileCode2 size={12}/><span>{name}</span><small>{name.split('.').pop()}</small></button>)}</div><div className="qpDataBlock"><div><Database size={12}/> DATA SOURCES</div><p><b>Personal trade model</b><span>connected</span></p><p><b>Market Data</b><span>server API</span></p><p><b>OpenAI Copilot</b><span>context-aware</span></p></div></aside><section className="qpEditor"><div className="qpEditorTabs"><div className="active"><FileCode2 size={12}/>{activeFile}<span>×</span></div><button onClick={addFile}><Plus size={12}/></button></div><div className="qpEditorBar"><span>personal / NQ edge research</span><div><small className={`qpRunState ${runState.status}`}>{runState.message}</small><button onClick={stageActive}><Play size={12}/> Stage</button></div></div><textarea className="qpCode qpCodeEditor" value={files[activeFile]} onChange={e=>setCode(e.target.value)} spellCheck="false"/><div className="qpNotebookOutput"><div className="qpOutputHead"><span>OUTPUT</span><b>personal_model.summary()</b><small>curated research snapshot</small></div><div className="qpOutputStats"><Stat label="OBSERVED EVAL WIN" value={fmtPct(personalModel.winProbability*100)} tone="good"/><Stat label="EXPECTANCY*" value={`${stats.ev.toFixed(2)}R`} sub="raw normalized eval scale"/><Stat label="PROFIT FACTOR*" value={stats.pf.toFixed(2)} sub="raw normalized eval scale"/><Stat label="TRADE SHARPE*" value={stats.tradeSharpe.toFixed(2)} sub="not annualized"/></div><div className="qpOutputChart"><ResponsiveContainer width="100%" height={190}><AreaChart data={equity}><defs><linearGradient id="qpeq" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#7a8190" stopOpacity=".24"/><stop offset="100%" stopColor="#7a8190" stopOpacity="0"/></linearGradient></defs><CartesianGrid stroke="#171a1e" vertical={false}/><XAxis dataKey="i" stroke="#4d5259" tick={{fontSize:8}}/><YAxis stroke="#4d5259" tick={{fontSize:8}} tickFormatter={v=>`$${Math.round(v/1000)}k`}/><Tooltip contentStyle={{background:'#080a0c',border:'1px solid #2b3036',fontSize:9}}/><Area type="monotone" dataKey="equity" stroke="#b6bbc3" fill="url(#qpeq)" strokeWidth={1}/></AreaChart></ResponsiveContainer></div></div><div className="qpStatus"><span>QNT draft workspace</span><span>editor writable</span><span>{activeFile}</span></div></section><aside className="qpCopilot"><div className="qpCopilotHead"><div><MessageSquareText size={14}/><b>QNT Copilot</b></div><button onClick={()=>{setMessages([]);setProposal(null)}}><Plus size={12}/> New chat</button></div><div className="qpSuggested">{suggestions.map(s=><button key={s} onClick={()=>send(s)}>{s}</button>)}</div><div className="qpChat">{messages.map((m,i)=>m.role==='user'?<div className="qpUserBubble" key={i}>{m.text}</div>:<div className="qpAgentBlock" key={i}><div><span className="qpAgentDot"/><b>QNT</b><small>OpenAI</small></div><p>{m.text}</p></div>)}{loading&&<div className="qpToolRow"><Search size={12}/><span>Reasoning over screen + file + model context</span><b>working</b></div>}{proposal&&<div className="qpProposal"><div><FileCode2 size={12}/><b>Editor update proposed</b><span>{proposal.file}</span></div><button onClick={applyProposal}>Apply to editor</button></div>}</div><div className="qpComposer"><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}} placeholder="Ask QNT to analyze, explain, compare, or propose a research edit…"/><div><span>OpenAI · screen + active-file context</span><button onClick={()=>send()} disabled={loading}>{loading?'Thinking…':'Send ↗'}</button></div></div></aside></div>;
}

function Verdict(){
  const payoffWins=personalModel.winR.map(r=>1.1*Math.log1p(Math.max(0,r))),avgWin=mean(payoffWins),avgLoss=Math.abs(mean(personalModel.lossR)),expectancy=personalModel.winProbability*avgWin-personalModel.lossProbability*avgLoss,top5=[...payoffWins].sort((a,b)=>b-a).slice(0,5),concentration=top5.reduce((a,b)=>a+b,0)/(payoffWins.reduce((a,b)=>a+b,0)||1)*100;
  return <div className="qpToolPage"><div className="qpToolTitle"><span>STRATEGY VALIDATION</span><h1>Verdict</h1><p>Transparent evidence review. No fake A–F grade or unexplained confidence score.</p></div><div className="qpVerdictHero"><div className="qpGrade">—</div><div><b>Positive modeled edge, limited evidence depth</b><p>The curated model has positive modeled expectancy, but the probability estimate is based on {personalModel.evalPositionIdeas} eval ideas and normalized R is not true stop-defined risk.</p></div></div><div className="qpDataMetrics"><Stat label="MODELED EXPECTANCY*" value={`${expectancy>=0?'+':''}${expectancy.toFixed(2)}R`} tone={expectancy>=0?'good':'bad'}/><Stat label="OBSERVED WIN RATE" value={fmtPct(personalModel.winProbability*100)}/><Stat label="AVG MODELED WIN*" value={`+${avgWin.toFixed(2)}R`}/><Stat label="AVG MODELED LOSS*" value={`-${avgLoss.toFixed(2)}R`}/><Stat label="TOP-5 WIN SHARE" value={fmtPct(concentration)} sub="payoff concentration"/></div><div className="qpFindings"><article><span className="good">●</span><div><b>Payoff asymmetry is present</b><p>Conservative transformed winners are larger than the remaining average loss.</p></div></article><article><span className="warn">●</span><div><b>Probability uncertainty is material</b><p>The Monte Carlo posterior varies win/loss/breakeven probabilities around the observed eval counts.</p></div></article><article><span className="warn">●</span><div><b>Winner concentration matters</b><p>The five largest conservative winners represent {concentration.toFixed(1)}% of total modeled winner magnitude, so tail dependence should be stress-tested.</p></div></article><article><span className="bad">●</span><div><b>True stop-defined R is unavailable</b><p>Normalized payoff units should not be interpreted as exact planned risk multiples.</p></div></article></div></div>;
}

function Regimes(){const steps=[['1','Join trades to futures bars','Match each real entry timestamp to verified NQ/MNQ market data.'],['2','Create measured features','Session, time of day, trend, realized volatility, volume and direction.'],['3','Require minimum sample','Show sample count and uncertainty before any regime comparison.'],['4','Publish only measured results','No regime WR/EV until the join is complete.']];return <div className="qpToolPage"><div className="qpToolTitle"><span>MARKET REGIMES</span><h1>Regime map</h1><p>No measured futures-regime dataset is connected, so QNT intentionally shows no fake performance numbers.</p></div><div className="qpRegimeEmpty"><BarChart3 size={20}/><div><b>Awaiting real futures context</b><p>Trade timestamps exist, but trend/volatility/volume features have not been joined from a verified historical futures source.</p></div></div><div className="qpFindings">{steps.map(([n,title,text])=><article key={n}><span className="violet">{n}</span><div><b>{title}</b><p>{text}</p></div></article>)}</div></div>}

export default function App(){
  const [tab,setTab]=useState('Workspace');
  const tabs=[['Workspace',Braces],['Prop Firm',Sigma],['Verdict',Activity],['Regimes',BarChart3],['Risk & Monte Carlo',BarChart3],['Volatility',Waves],['Data',Database]];
  useEffect(()=>{const nav=e=>{if(e.detail?.type==='research'&&tabs.some(([name])=>name===e.detail.tab))setTab(e.detail.tab)};window.addEventListener('qnt:navigate',nav);return()=>window.removeEventListener('qnt:navigate',nav)},[]);
  return <div className="appShell qpCore"><header className="topNav qpResearchNav"><nav>{tabs.map(([name,Icon])=><button key={name} className={tab===name?'active':''} onClick={()=>setTab(name)}><Icon size={12}/>{name}</button>)}</nav><div className="rightNav"><span className="statusDot"/> PERSONAL MODEL</div></header><main>{tab==='Workspace'&&<Workspace/>}{tab==='Verdict'&&<Verdict/>}{tab==='Regimes'&&<Regimes/>}{tab==='Volatility'&&<VolatilityLab/>}{tab==='Data'&&<DataTerminal/>}{(tab==='Prop Firm'||tab==='Risk & Monte Carlo')&&<div className="qpLoadingTool"><Settings2 size={15}/><span>Opening simulation workspace…</span></div>}</main></div>;
}
